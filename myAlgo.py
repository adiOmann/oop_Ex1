from math import ceil

def allocate
    for i in elev_List:
    absdjbasidjb




















def allocate (self):
    while len(self.allCalls) > 0:
         for elev in self.building.elevators:
            for i in range(ceil(elev.speed)):
                if i < len(self.allCalls):
                    if self.allCalls[i].src < self.building.minFloor or self.allCalls[i].src > self.building.maxFloor or self.allCalls[i].dest > self.building.maxFloor:
                        self.allCalls.remove(self.allCalls[i])


